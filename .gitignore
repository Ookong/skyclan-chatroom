config.json
.last-read
.heartbeat
.pending-*
node_modules/
.wrangler/
.DS_Store

# Runtime state files (created by poll script)
.last-night-poll
.last-read
.heartbeat
.pending-*
.tokens
config.json.bak
/docs/review-logs
# v4.1 ack-guard 状态文件（含消息内容，绝不入库）
.pending-*
.lost-*
config.json.bak*
.poll-notes.log
# v6 trigger.js 状态文件（独立游标 + 审计日志）
.trigger-last-read
.trigger-fired-log

# Env files (never commit keys)
*.env
.env
.env.*
!.env.example
