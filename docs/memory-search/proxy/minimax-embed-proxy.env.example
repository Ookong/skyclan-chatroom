# MiniMax Embedding 代理环境变量模板
# 复制为 ~/.openclaw/scripts/minimax-embed-proxy.env，填入真实 key，chmod 600
# ⚠️ 不要把真实 key commit 进仓库

MINIMAX_API_KEY=sk-cp-REPLACE_ME
PROXY_PORT=9999
EMBED_TYPE=db
MIN_INTERVAL_MS=200
MAX_RETRIES=4
